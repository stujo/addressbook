<div class="page-header">
  <h1>Contacts: Over 50's</h1>
</div>
<%= render partial: 'contacts_table', locals: {contacts: @contacts} %>

<div>
  <%= link_to new_contact_path, :class => 'btn btn-primary' do %>
      <span class="glyphicon glyphicon-plus"></span>
      Add Contact
  <% end %>
</div>
